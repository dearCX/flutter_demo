import 'package:flutter/material.dart';
class AlipayScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Alipay'),
      ),
      body: Center(
        child: Text('Alipay page'),
      ),
    );
  }
}